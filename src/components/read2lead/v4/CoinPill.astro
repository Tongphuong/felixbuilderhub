---
export interface Props {
  id?: string;
  value?: number | string;
}

const { id, value = 0 } = Astro.props;
---

<span class="r2l-kid-coin-pill" id={id}>
  🪙 <span data-r2l-coin-value>{value}</span>
</span>
