---
const code = (Astro.url.searchParams.get('code') || '').trim().toUpperCase();
const target = code ? `/read2lead/review?code=${encodeURIComponent(code)}` : '/read2lead/review';
return Astro.redirect(target);
---
