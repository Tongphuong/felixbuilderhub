/* Felix Coaching website UI kit — page screens.
   Recreates the production homepage and coaching page using DS primitives. */

(() => {
const { Button, Card, Badge, Avatar, SectionHeading, Faq } = window.FelixCoachingDesignSystem_05c199;

const wrap = { maxWidth: 'var(--container)', margin: '0 auto', padding: '0 var(--gutter)' };

function Hero({ onNav }) {
  return (
    <section style={{ ...wrap, padding: '80px var(--gutter)', textAlign: 'center' }}>
      <Badge variant="accent">Dịch vụ chính · Nhóm nhỏ tối đa 4 bé · Online</Badge>
      <h1 style={{ margin: '24px auto 0', maxWidth: 880, fontFamily: 'var(--font-display)', fontWeight: 800, fontSize: 'var(--text-display-lg)', lineHeight: 1.3, color: 'var(--cream)' }}>
        Huấn luyện kĩ năng nói<br /><span style={{ color: 'var(--accent)' }}>rèn tự tin cho con</span>
      </h1>
      <p style={{ margin: '24px auto 0', maxWidth: 620, fontSize: 'var(--text-lg)', color: 'var(--cream-muted)', lineHeight: 1.625 }}>
        Felix dùng tiếng Anh làm phương tiện — kể chuyện, thuyết trình, tranh luận trong lớp nhóm tối đa 4 bé, online.
      </p>
      <div style={{ marginTop: 40, display: 'flex', gap: 16, justifyContent: 'center', flexWrap: 'wrap' }}>
        <Button size="lg" onClick={() => onNav('contact')}>Đặt lịch tư vấn miễn phí →</Button>
        <Button size="lg" variant="secondary" onClick={() => onNav('hoc-sinh')}>Xem tiến độ con</Button>
      </div>
      <p style={{ marginTop: 24, fontSize: 'var(--text-sm)', color: 'var(--cream-dim)' }}>Hơn 10 năm coach huấn luyện kĩ năng nói cho trẻ em Việt</p>
    </section>
  );
}

function About() {
  return (
    <section style={{ background: 'var(--surface-section)', padding: '64px var(--gutter)' }}>
      <div style={{ maxWidth: '64rem', margin: '0 auto', display: 'grid', gridTemplateColumns: 'auto 1fr', gap: 40, alignItems: 'center' }} className="about-grid">
        <Avatar src="../../assets/felix.jpg" alt="Thầy Phương" size="xl" />
        <div>
          <h2 style={{ fontFamily: 'var(--font-display)', fontWeight: 700, fontSize: 'var(--text-display-sm)', color: 'var(--cream)' }}>Felix Coaching là gì?</h2>
          <p style={{ marginTop: 16, color: 'var(--cream-muted)', lineHeight: 1.625 }}>
            Tôi là Phương — coach huấn luyện kĩ năng nói, sử dụng tiếng Anh làm phương tiện. Tôi rèn <strong style={{ color: 'var(--cream)' }}>tự tin nói thật</strong>: con dám mở miệng, kể chuyện có cấu trúc, thuyết trình trước lớp và tranh luận có lý lẽ.
          </p>
          <p style={{ marginTop: 12, color: 'var(--cream-muted)', lineHeight: 1.625 }}>
            Ngoài lớp coaching, tôi xây dựng <strong style={{ color: 'var(--cream)' }}>Read2Lead</strong> và <strong style={{ color: 'var(--cream)' }}>MSMW</strong> — sản phẩm giáo dục tăng cường bởi AI.
          </p>
        </div>
      </div>
    </section>
  );
}

function Products({ onNav }) {
  return (
    <section style={{ ...wrap, padding: '80px var(--gutter)' }}>
      <SectionHeading eyebrow="Bổ trợ ngoài lớp" title="Sản phẩm giáo dục tăng cường bởi AI"
        lead="Felix dùng AI để tạo bài luyện đọc và nghe cá nhân hoá — bổ trợ buổi coaching, không thay thế lớp trực tiếp." />
      <Card variant="accent" style={{ marginTop: 32, padding: 32 }}>
        <div style={{ display: 'flex', gap: 24, flexWrap: 'wrap', alignItems: 'center', justifyContent: 'space-between' }}>
          <div style={{ maxWidth: 560 }}>
            <div style={{ display: 'flex', gap: 12, alignItems: 'center', flexWrap: 'wrap' }}>
              <h3 style={{ fontFamily: 'var(--font-display)', fontWeight: 700, fontSize: 'var(--text-h3)', color: 'var(--cream)' }}>Read2Lead</h3>
              <Badge variant="gold">Thử nghiệm — miễn phí</Badge>
            </div>
            <p style={{ marginTop: 16, color: 'var(--cream-muted)', lineHeight: 1.625 }}>
              Công cụ luyện đọc và nghe tăng cường bởi AI — con chọn chủ đề, làm nhiệm vụ, tích điểm và leo cấp.
            </p>
          </div>
          <Button size="lg" onClick={() => onNav('read2lead')}>Tạo bài miễn phí →</Button>
        </div>
      </Card>
    </section>
  );
}

function MSMW() {
  return (
    <section style={{ background: 'var(--surface-section)', padding: '80px var(--gutter)' }}>
      <div style={{ maxWidth: 'var(--container)', margin: '0 auto', display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 40, alignItems: 'center' }} className="msmw-grid">
        <div>
          <p className="fx-eyebrow">Sản phẩm riêng</p>
          <h2 style={{ marginTop: 8, fontFamily: 'var(--font-display)', fontWeight: 700, fontSize: 'var(--text-display-sm)', color: 'var(--cream)' }}>MSMW — My Story, My World</h2>
          <p style={{ marginTop: 16, color: 'var(--cream-muted)', lineHeight: 1.625 }}>
            Sách truyện song ngữ Việt–Anh với <strong style={{ color: 'var(--cream)' }}>chính con là nhân vật chính</strong> — Felix dùng AI tạo minh hoạ riêng từ ảnh thật của bé.
          </p>
          <p style={{ marginTop: 12, fontSize: 'var(--text-sm)', color: 'var(--cream-dim)' }}>Bản số từ 69k · Bản in từ 129k · Giao toàn quốc</p>
          <div style={{ marginTop: 24 }}><Button variant="secondary">Xem trang MSMW →</Button></div>
        </div>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}>
          <img src="../../assets/msmw-toy-brick.jpg" alt="Bìa sách MSMW" style={{ borderRadius: 'var(--radius-xl)', border: '1px solid var(--border-subtle)', boxShadow: 'var(--shadow-card)', width: '100%' }} />
          <img src="../../assets/msmw-heart-garden.jpg" alt="Bìa sách MSMW" style={{ marginTop: 32, borderRadius: 'var(--radius-xl)', border: '1px solid var(--border-subtle)', boxShadow: 'var(--shadow-card)', width: '100%' }} />
        </div>
      </div>
    </section>
  );
}

function Faqs() {
  const items = [
    ['Lớp học có bao nhiêu bạn?', 'Nhóm nhỏ tối đa 4 học viên. Học online qua Zoom hoặc Google Meet — mỗi bạn có nhiều thời gian nói và Felix theo sát từng bạn.'],
    ['Bé chưa biết gì có học được không?', 'Có. Felix có lộ trình riêng cho beginner — bắt đầu bằng hội thoại đơn giản, không grammar khô khan.'],
    ['Felix có kinh nghiệm bao lâu?', 'Hơn 10 năm coach huấn luyện kĩ năng nói cho trẻ em, sử dụng tiếng Anh làm phương tiện.'],
  ];
  return (
    <section style={{ maxWidth: 'var(--container-sm)', margin: '0 auto', padding: '80px var(--gutter)' }}>
      <SectionHeading align="center" title="Câu hỏi thường gặp" />
      <div style={{ marginTop: 32, display: 'grid', gap: 12 }}>
        {items.map(([q, a], i) => <Faq key={i} question={q} open={i === 0}>{a}</Faq>)}
      </div>
    </section>
  );
}

function HomeScreen({ onNav }) {
  return (
    <div>
      <Hero onNav={onNav} />
      <About />
      <Products onNav={onNav} />
      <MSMW />
      <Faqs />
    </div>
  );
}

Object.assign(window, { HomeScreen });
})();
